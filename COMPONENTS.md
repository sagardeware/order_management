# Installed Shadcn/UI Components

This project has **47 shadcn/ui components** pre-installed and ready to use.

## ✅ Complete Component List

### 1. Accordion (`accordion.tsx`)
Collapsible sections for organizing content
```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion"
```

### 2. Alert Dialog (`alert-dialog.tsx`)
Modal dialog for important confirmations
```tsx
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog"
```

### 3. Alert (`alert.tsx`)
Display important messages to users
```tsx
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
```

### 4. Avatar (`avatar.tsx`)
Display user profile pictures
```tsx
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"
```

### 5. Badge (`badge.tsx`)
Small status indicators
```tsx
import { Badge } from "@/components/ui/badge"
```

### 6. Breadcrumb (`breadcrumb.tsx`)
Navigation breadcrumbs
```tsx
import { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from "@/components/ui/breadcrumb"
```

### 7. Button (`button.tsx`)
Interactive button component
```tsx
import { Button } from "@/components/ui/button"
```

### 8. Calendar (`calendar.tsx`)
Date picker calendar
```tsx
import { Calendar } from "@/components/ui/calendar"
```

### 9. Card (`card.tsx`)
Content container with header and footer
```tsx
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/card"
```

### 10. Carousel (`carousel.tsx`)
Image and content carousel
```tsx
import { Carousel, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from "@/components/ui/carousel"
```

### 11. Chart (`chart.tsx`)
Data visualization charts
```tsx
import { ChartContainer, ChartTooltip, ChartTooltipContent } from "@/components/ui/chart"
```

### 12. Checkbox (`checkbox.tsx`)
Checkbox input
```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

### 13. Collapsible (`collapsible.tsx`)
Collapsible content sections
```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible"
```

### 14. Command (`command.tsx`)
Command palette / search
```tsx
import { Command, CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList, CommandSeparator, CommandShortcut } from "@/components/ui/command"
```

### 15. Context Menu (`context-menu.tsx`)
Right-click context menu
```tsx
import { ContextMenu, ContextMenuContent, ContextMenuItem, ContextMenuTrigger } from "@/components/ui/context-menu"
```

### 16. Dialog (`dialog.tsx`)
Modal dialog
```tsx
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
```

### 17. Drawer (`drawer.tsx`)
Bottom drawer panel
```tsx
import { Drawer, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerTitle, DrawerTrigger } from "@/components/ui/drawer"
```

### 18. Dropdown Menu (`dropdown-menu.tsx`)
Dropdown menu
```tsx
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu"
```

### 19. Form (`form.tsx`)
Form with validation
```tsx
import { Form, FormControl, FormDescription, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form"
```

### 20. Hover Card (`hover-card.tsx`)
Rich hover content
```tsx
import { HoverCard, HoverCardContent, HoverCardTrigger } from "@/components/ui/hover-card"
```

### 21. Input OTP (`input-otp.tsx`)
One-time password input
```tsx
import { InputOTP, InputOTPGroup, InputOTPSeparator, InputOTPSlot } from "@/components/ui/input-otp"
```

### 22. Input (`input.tsx`)
Text input field
```tsx
import { Input } from "@/components/ui/input"
```

### 23. Label (`label.tsx`)
Form label
```tsx
import { Label } from "@/components/ui/label"
```

### 24. Menubar (`menubar.tsx`)
Application menu bar
```tsx
import { Menubar, MenubarContent, MenubarItem, MenubarMenu, MenubarTrigger } from "@/components/ui/menubar"
```

### 25. Navigation Menu (`navigation-menu.tsx`)
Horizontal navigation
```tsx
import { NavigationMenu, NavigationMenuContent, NavigationMenuItem, NavigationMenuLink, NavigationMenuList, NavigationMenuTrigger } from "@/components/ui/navigation-menu"
```

### 26. Pagination (`pagination.tsx`)
Page navigation
```tsx
import { Pagination, PaginationContent, PaginationItem, PaginationLink, PaginationNext, PaginationPrevious } from "@/components/ui/pagination"
```

### 27. Popover (`popover.tsx`)
Floating popover content
```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
```

### 28. Progress (`progress.tsx`)
Progress indicator
```tsx
import { Progress } from "@/components/ui/progress"
```

### 29. Radio Group (`radio-group.tsx`)
Radio button group
```tsx
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

### 30. Resizable (`resizable.tsx`)
Resizable panels
```tsx
import { ResizableHandle, ResizablePanel, ResizablePanelGroup } from "@/components/ui/resizable"
```

### 31. Scroll Area (`scroll-area.tsx`)
Custom scrollbar
```tsx
import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area"
```

### 32. Select (`select.tsx`)
Dropdown select
```tsx
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
```

### 33. Separator (`separator.tsx`)
Visual divider
```tsx
import { Separator } from "@/components/ui/separator"
```

### 34. Sheet (`sheet.tsx`)
Slide-out panel
```tsx
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle, SheetTrigger } from "@/components/ui/sheet"
```

### 35. Sidebar (`sidebar.tsx`)
Collapsible sidebar navigation (23KB - largest component!)
```tsx
import { Sidebar, SidebarContent, SidebarFooter, SidebarGroup, SidebarHeader, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar"
```

### 36. Skeleton (`skeleton.tsx`)
Loading placeholder
```tsx
import { Skeleton } from "@/components/ui/skeleton"
```

### 37. Slider (`slider.tsx`)
Range slider
```tsx
import { Slider } from "@/components/ui/slider"
```

### 38. Sonner (`sonner.tsx`)
Toast notifications (Sonner library)
```tsx
import { Toaster } from "@/components/ui/sonner"
```

### 39. Switch (`switch.tsx`)
Toggle switch
```tsx
import { Switch } from "@/components/ui/switch"
```

### 40. Table (`table.tsx`)
Data table
```tsx
import { Table, TableBody, TableCaption, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
```

### 41. Tabs (`tabs.tsx`)
Tabbed interface
```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
```

### 42. Textarea (`textarea.tsx`)
Multi-line text input
```tsx
import { Textarea } from "@/components/ui/textarea"
```

### 43. Toast (`toast.tsx`)
Toast notifications
```tsx
import { Toast, ToastAction, ToastClose, ToastDescription, ToastProvider, ToastTitle, ToastViewport } from "@/components/ui/toast"
```

### 44. Toaster (`toaster.tsx`)
Toast container
```tsx
import { Toaster } from "@/components/ui/toaster"
```

### 45. Toggle Group (`toggle-group.tsx`)
Toggle button group
```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
```

### 46. Toggle (`toggle.tsx`)
Toggle button
```tsx
import { Toggle } from "@/components/ui/toggle"
```

### 47. Tooltip (`tooltip.tsx`)
Hover tooltip
```tsx
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip"
```

## 📊 Component Statistics

- **Total Components**: 47
- **Largest Component**: Sidebar (23.6 KB)
- **Total Size**: ~150 KB
- **All components are TypeScript-ready**
- **All components support dark mode**
- **All components are accessible (ARIA compliant)**

## 🎯 Usage Example

```tsx
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"

export default function Example() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>User Profile</CardTitle>
        <CardDescription>Manage your account settings</CardDescription>
      </CardHeader>
      <CardContent className="flex items-center gap-4">
        <Avatar>
          <AvatarImage src="https://github.com/shadcn.png" />
          <AvatarFallback>CN</AvatarFallback>
        </Avatar>
        <div>
          <h3 className="font-semibold">John Doe</h3>
          <Badge>Premium</Badge>
        </div>
        <Button className="ml-auto">Edit Profile</Button>
      </CardContent>
    </Card>
  )
}
```

## 🔗 Resources

- [Shadcn/UI Documentation](https://ui.shadcn.com)
- [Component Examples](https://ui.shadcn.com/examples)
- [Themes](https://ui.shadcn.com/themes)

---

All components are ready to use! Just import and start building. 🚀
